services:
  bank:
    build: .
    container_name: bank
    restart: unless-stopped
    ports:
      - "5000:8080"
    volumes:
      - bank_data:/app/data
    environment:
      - RUST_LOG=info

volumes:
  bank_data:
